---
"lrclib-api": minor
"@lrclib-api/react": patch
---

Harden URL and token handling, add timeouts and validated responses, make tests deterministic, improve LRC parsing and React request cancellation, and repair package metadata and release checks.
