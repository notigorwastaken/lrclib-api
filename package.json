{
  "name": "@lrclib-api/repo",
  "version": "0.0.0",
  "main": "index.js",
  "private": true,
  "directories": {
    "doc": "docs"
  },
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo run test",
    "lint": "turbo lint",
    "format": "turbo format",
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "publish": "turbo run build format lint && changeset version && changeset publish"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.0.8",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@changesets/cli": "^2.29.6"
  }
}
